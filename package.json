{
  "name": "vaultstream",
  "version": "0.0.1",
  "private": true,
  "description": "VaultStream - Secure encrypted file vault platform on AWS",
  "workspaces": [
    "packages/shared",
    "packages/api",
    "packages/lambdas",
    "packages/frontend",
    "infra"
  ],
  "scripts": {
    "dev": "docker compose up -d && concurrently \"npm:dev:api\" \"npm:dev:frontend\"",
    "dev:api": "npm run dev --workspace=packages/api",
    "dev:frontend": "npm run dev --workspace=packages/frontend",
    "dev:infra": "docker compose up -d",
    "dev:seed": "npx tsx scripts/seed-data.ts",
    "dev:stop": "docker compose down",
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint . --ext .ts,.tsx",
    "lint:fix": "eslint . --ext .ts,.tsx --fix",
    "format": "prettier --write \"**/*.{ts,tsx,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,json,md}\"",
    "typecheck": "tsc --noEmit --project tsconfig.base.json",
    "bundle:lambdas": "node scripts/bundle-lambdas.mjs",
    "deploy:lambda-code": "node scripts/deploy-lambda-code.mjs",
    "build:sharp-layer": "node scripts/build-sharp-layer.mjs",
    "deploy:sharp-layer": "node scripts/deploy-sharp-layer.mjs"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "concurrently": "^8.2.0",
    "esbuild": "^0.21.5",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-import": "^2.29.0",
    "eslint-import-resolver-typescript": "3.5.5",
    "prettier": "^3.2.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "@vitest/coverage-v8": "^1.6.0",
    "fast-check": "^3.15.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
